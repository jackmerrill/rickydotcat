version: '3.7'

services: 
  backend:
    image: rickycat-backend
    ports: 
      - "5000:5000"
    volumes:
      - "/root/NewRickyCat/post_img:/usr/src/app/posts"
  frontend:
    image: rickycat-frontend
    ports:
      - "4000:3000"